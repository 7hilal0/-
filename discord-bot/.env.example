# Copy this file to .env and fill in values
DISCORD_TOKEN=
# Optional: limit command sync to a single test guild for faster updates
# GUILD_ID=
# Optional: expose a health endpoint for platforms that need HTTP pings
ENABLE_HEALTH_SERVER=true
PORT=8080
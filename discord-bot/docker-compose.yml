version: "3.8"
services:
  bot:
    build: .
    container_name: discord-bot
    env_file:
      - .env
    restart: always
    # Uncomment if your platform requires a web port open for health checks
    # ports:
    #   - "8080:8080"
    # healthcheck:
    #   test: ["CMD", "curl", "-f", "http://localhost:8080/healthz"]
    #   interval: 30s
    #   timeout: 5s
    #   retries: 3